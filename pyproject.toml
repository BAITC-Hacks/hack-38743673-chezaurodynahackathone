[project]
name = "smartmatch-hackalem"
version = "1.1.0"
description = "SmartMatch MVP: explainable contractor matching with SQLite and server-side AI"
requires-python = ">=3.11"
dependencies = []

[tool.ruff]
line-length = 120

